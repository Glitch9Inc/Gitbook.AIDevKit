---
description: Define provider-specific custom tools when standard types are not enough
---

# Custom Tool

`Custom` represents provider-specific or app-defined tool contracts outside the standard built-in types.

Use it when your integration requires a custom schema or specialized behavior.
